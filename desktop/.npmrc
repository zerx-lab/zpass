# Electron Forge requires a flat node_modules layout. Without hoisting,
# Forge refuses to package because Electron's packager cannot find
# dependencies through pnpm's symlinked store.
node-linker=hoisted
