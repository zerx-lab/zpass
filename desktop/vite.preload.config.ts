import { defineConfig } from "vite";

// Electron preload script build. The Forge Vite plugin injects the entry
// and CJS-output settings; we only add overrides here.
export default defineConfig({});
