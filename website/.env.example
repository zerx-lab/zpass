# ZPass 官网环境变量示例
# 复制为 .env 并按环境填写；standalone Node server 部署时通过进程环境变量注入。

# 后端 API 网关 base（含 /v1，不含末尾斜杠）。定价页 SSR 据此从公开端点
# `${ZPASS_API_BASE}/plans/public` 拉取套餐，与 SaaS 运营平台同源 plans 表。
# 缺省（不设此变量）时使用生产网关。
#
#   生产：https://zpass-app.zerx.dev/v1   （= operator API https://zpass-app.zerx.dev/v1/operator 的同网关）
#   开发：本地后端，默认 http://127.0.0.1:8080/v1（zpass_cloud BIND_ADDR=0.0.0.0:8080）
#
# 运行时 process.env 优先级高于构建期注入，部署时改这里即可切换后端。
ZPASS_API_BASE=http://127.0.0.1:8080/v1
